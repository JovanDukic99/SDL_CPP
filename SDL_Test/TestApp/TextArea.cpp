#include "TextArea.h"

TextArea::TextArea(std::string text, glm::ivec2 position, Font font, SDL_Renderer* renderer) : text(text), position(position), font(font), renderer(renderer) {

}

void TextArea::draw() {

}
